# Robot Framework tests

Install Robot framework tests with `uv`.

Use following command to run tests and store results under results folder:

```bash
uv run robot -d results suites
```

Make sure the app is running before running tests.
